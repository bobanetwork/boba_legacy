service: sls-bobaopera-metaTransaction # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs12.x
  stackName: sls-bobaopera-metaTransaction
  stage: prod
  region: us-east-1
  role: ${file(env-bobaopera.yml):ROLE}

package:
  exclude:
    - .gitignore
    - node_modules
  individually: true

functions:
  boba_swapSecondaryFeeToken:
    handler: metaTransaction_swapNativeTokenForBOBA.mainnetHandler
    memorySize: 10240 # optional, in MB, default is 1024
    timeout: 60 # optional, in seconds, default is 6
    vpc:
      securityGroupIds:
        - ${file(env-bobaopera.yml):SECURITY_GROUPS}
      subnetIds:
        - ${file(env-bobaopera.yml):SUBNET_ID_1}
        - ${file(env-bobaopera.yml):SUBNET_ID_2}
    events:
      - http:
          path: send.swapSecondaryFeeToken
          method: post
          cors: true
    layers:
      - ${file(env-bobaopera.yml):LAYERS}

