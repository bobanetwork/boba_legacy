service: sls-bobabnb-metaTransaction # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs12.x
  stackName: sls-bobabnb-metaTransaction
  stage: prod
  region: us-east-1
  role: ${file(env-bobabnb.yml):ROLE}

package:
  exclude:
    - .gitignore
    - node_modules
  individually: true

functions:
  boba_swapSecondaryFeeToken:
    handler: metaTransaction_swapNativeTokenForBOBA.mainnetHandler
    memorySize: 10240 # optional, in MB, default is 1024
    timeout: 60 # optional, in seconds, default is 6
    vpc:
      securityGroupIds:
        - ${file(env-bobabnb.yml):SECURITY_GROUPS}
      subnetIds:
        - ${file(env-bobabnb.yml):SUBNET_ID_1}
        - ${file(env-bobabnb.yml):SUBNET_ID_2}
    events:
      - http:
          path: send.swapSecondaryFeeToken
          method: post
          cors: true
    layers:
      - ${file(env-bobabnb.yml):LAYERS}

