service: sls-bobabeam-metaTransaction # NOTE: update this with your service name

provider:
  name: aws
  runtime: nodejs14.x
  stackName: sls-bobabeam-metaTransaction
  stage: prod
  region: us-east-1

package:
  exclude:
    - .gitignore
    - node_modules
  individually: true

functions:
  boba_swapSecondaryFeeToken:
    handler: metaTransaction_swapNativeTokenForBOBA.mainnetHandler
    memorySize: 10240 # optional, in MB, default is 1024
    timeout: 60 # optional, in seconds, default is 6
    events:
      - http:
          path: send.swapSecondaryFeeToken
          method: post
          cors: true
    layers:
      - ${file(env.yml):LAYERS}

