{
  "name": "@turing/monorepo",
  "version": "1.0.0",
  "keywords": [
    "ethereum",
    "react",
    "workspaces",
    "yarn"
  ],
  "private": true,
  "scripts": {
    "subgraph:auth": "yarn workspace @turing/subgraph auth",
    "subgraph:codegen": "yarn workspace @turing/subgraph codegen",
    "subgraph:build": "yarn workspace @turing/subgraph build",
    "subgraph:deploy": "yarn workspace @turing/subgraph deploy",
    "react-app:build": "yarn workspace @turing/react-app build",
    "react-app:eject": "yarn workspace @turing/react-app eject",
    "react-app:ipfs": "yarn workspace @turing/react-app ipfs",
    "react-app:start": "yarn workspace @turing/react-app start",
    "react-app:test": "yarn workspace @turing/react-app test"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
